نسخه نهایی پایش رجا + بله
==========================

این نسخه شامل این اصلاحات است:
- انتخاب تاریخ با کلیک واقعی روی تقویم رجا
- تشخیص واقعی کارت قطار و دکمه «رزرو بلیت»
- پایش مستقل هر روز: خراب شدن یک تاریخ، کل بازه را fail نمی‌کند
- هشدار دوباره اگر ظرفیت یک تاریخ تمام شود و بعداً دوباره برگردد
- کاهش نمایش توکن بله در لاگ‌های HTTP
- زمان‌بندی دور بعد بر اساس شروع دور قبلی، نه پایان آن

تست محلی
---------
با .venv فعال:

    python -m tools.test_raja_check

برای تهران -> شیراز، 1405/06/27 باید available: True ببینی.

انتقال به GitHub
----------------
اگر از فایل production patch استفاده می‌کنی، این سه فایل را در repository جایگزین کن:

    app/raja.py
    app/main.py
    app/storage.py

سپس Commit کن.

به‌روزرسانی VPS
----------------
داخل VNC:

    cd /root/raja-bale-alert-bot
    git pull
    docker compose up -d --build

بعد:

    docker compose ps

و:

    docker compose logs --tail=100

در لاگ عادی باید پیام Worker started را ببینی.

توجه درباره دیتابیس
-------------------
لازم نیست data/bot.sqlite3 را پاک کنی.
جدول جدید watch_date_state هنگام اجرای نسخه جدید به صورت خودکار ساخته می‌شود.
پایش‌های فعلی باقی می‌مانند.

نکته درباره اولین هشدار بعد از آپدیت
-----------------------------------
چون نسخه جدید وضعیت هر «تاریخ» را جدا ذخیره می‌کند، اگر هنگام اولین اجرای نسخه جدید
در یکی از تاریخ‌ها بلیت موجود باشد، ممکن است یک هشدار تازه دریافت کنی. بعد از آن
هشدارها بر اساس تغییر وضعیت هر تاریخ کنترل می‌شوند.

نکته درباره «هر 60 ثانیه»
--------------------------
برنامه دور بعدی را تقریباً 60 ثانیه بعد از شروع دور قبلی برنامه‌ریزی می‌کند.
اگر بررسی کل بازه خودش بیش از 60 ثانیه طول بکشد، اجرای بعدی بلافاصله پس از
اتمام دور قبلی (با چند ثانیه فاصله) شروع می‌شود؛ بنابراین برای بازه‌های خیلی
بزرگ، از نظر فیزیکی نمی‌توان تک‌تک همه روزها را دقیقاً در هر 60 ثانیه بررسی کرد.
