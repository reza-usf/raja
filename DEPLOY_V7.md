Bale Dashboard v7 — Deployment
==============================

رفتار نسخه
----------
برای هر پایش یک Dashboard ثابت دارد:
- همه تاریخ‌های بازه را نشان می‌دهد.
- هر دور همان پیام edit می‌شود.
- اگر بلیت نباشد: ❌ بلیت موجود نیست
- اگر بلیت باشد: اطلاعات قطار و ظرفیت نمایش داده می‌شود.
- اگر یک تاریخ خطا دهد: ⚠️ خطا در بررسی

Notification جدید فقط برای این موارد ارسال می‌شود:
1) تاریخ از «بدون بلیت» به «دارای بلیت» تبدیل شود.
2) یک قطار جدید به تاریخ دارای بلیت اضافه شود.
3) ظرفیت دقیق یک قطار برای نخستین بار به کمتر از 10 بلیت برسد.

تغییرات عادی ظرفیت، قیمت، یا timestamp فقط Dashboard را edit می‌کنند و
پیام جدید نمی‌فرستند.

قبل از VPS
----------
فایل‌های این Patch از نسخه Local v6 که تست کردی استفاده می‌کنند.
تست منطق دیتابیس/اعلان نیز اجرا شده است.

فایل‌های GitHub
---------------
این فایل‌ها را در repository جایگزین/اضافه کن:

app/raja.py
app/status_logic.py
app/storage.py
app/main.py
tools/test_status_logic.py
tools/watch_local.py
tools/test_production_dashboard.py

VPS
---
بعد از Commit در GitHub:

cd /root/raja-bale-alert-bot
git pull
docker compose up -d --build
docker compose ps
docker compose logs --tail=100

نیازی به حذف data/bot.sqlite3 نیست.
نسخه جدید ستون‌ها و جدول‌های لازم را روی دیتابیس موجود به صورت خودکار migrate می‌کند.

پایش‌های قدیمی
--------------
پایش فعلی تو باقی می‌ماند.
چون پایش قدیمی هنوز dashboard_message_id ندارد، در اولین دور نسخه جدید یک
Dashboard جدید برای آن ارسال می‌شود و از دورهای بعد همان پیام edit می‌شود.

برای یک پایش تازه، Dashboard بلافاصله هنگام ساخت ایجاد می‌شود.

نکته زمان
---------
اگر اسکن 5 روز مثلاً 15 ثانیه طول بکشد، برنامه حدود 45 ثانیه صبر می‌کند و
دور بعد تقریباً 60 ثانیه بعد از شروع دور قبلی شروع می‌شود.
اگر خود اسکن بیشتر از 60 ثانیه طول بکشد، دورها همزمان اجرا نمی‌شوند.
