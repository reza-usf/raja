Production Dashboard v7
=======================

این نسخه همان منطق Local v6 را به بات بله وصل می‌کند.

رفتار نهایی
-----------
برای هر پایش یک پیام Dashboard ثابت وجود دارد:

- هر دور همان پیام با editMessageText به‌روزرسانی می‌شود.
- برای هر تاریخ، موجود / ناموجود / خطای بررسی نشان داده می‌شود.
- تغییر عادی ظرفیت پیام جدید تولید نمی‌کند.
- رویداد مهم یک پیام جدید sendMessage تولید می‌کند و Notification می‌دهد:
  1) بلیت از ناموجود به موجود تبدیل شود.
  2) یک قطار جدید به نتایج موجود اضافه شود.
  3) ظرفیت دقیق یک قطار از حالت 10 یا بیشتر به کمتر از 10 برسد.

اگر Dashboard توسط کاربر پاک شود یا دیگر قابل Edit نباشد، بات یک Dashboard جدید
می‌سازد و message_id جدید را ذخیره می‌کند.

بهینه‌سازی سرعت
---------------
تاریخ اول با جستجوی کامل رجا باز می‌شود. تاریخ‌های بعدی بازه با دکمه «روز بعد»
صفحه نتایج بررسی می‌شوند. اگر این روش برای یک روز شکست بخورد، برای همان روز به
جستجوی کامل برمی‌گردد.

فایل‌های Patch
--------------
app/raja.py
app/status_logic.py
app/storage.py
app/main.py
tools/test_status_logic.py
tools/watch_local.py

روش نصب روی GitHub
------------------
فایل‌های بالا را در repository فعلی جایگزین/اضافه کن و Commit کن.

VPS
---
بعد در VNC:

    cd /root/raja-bale-alert-bot
    git pull
    docker compose up -d --build
    docker compose ps
    docker compose logs --tail=100

لازم نیست دیتابیس را پاک کنی.
نسخه جدید ستون dashboard_message_id و جدول watch_date_snapshot را به صورت
خودکار ایجاد می‌کند.

پایش‌های قبلی
-------------
پایش فعال قبلی باقی می‌ماند. چون هنوز Dashboard message_id ندارد، در اولین دور
نسخه جدید یک Dashboard تازه برای آن ارسال می‌شود.

تست بعد از Deploy
-----------------
برای پایش 27 تا 31 شهریور باید:
- یک Dashboard دریافت کنی.
- حدود هر دقیقه ساعت «آخرین بررسی» آن تغییر کند.
- برای روز بدون بلیت عبارت «بلیت موجود نیست» ببینی.
- برای روز دارای بلیت، قطار و ظرفیت را ببینی.
- در دورهای بدون تغییر مهم، پیام جدیدی دریافت نکنی.
